import styled from "styled-components";

export default styled.div`
  font-size: ${36 / 16}rem;
  letter-spacing: 2px;
  font-family: 'firaCodeBold';
  color: currentColor;
  margin-right: auto;
  pointer-events: none;
  user-select: none;
`;
import styled from "styled-components";

export default styled.div`
  font-size: ${64 / 16}rem;
  font-family: 'thunder';
  color: currentColor;
  margin-right: auto;
  pointer-events: none;
  user-select: none;
  display: inline-flex;
  position: relative;
  top: 4px;
`;